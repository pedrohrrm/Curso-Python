#Usamos o FOR quando já sabemos quantas repetições vão ocorrer no laço.

texto = 'Python'

#letra foi criada a partir do momento que estamos iterando o texto, conforme trecho de código abaixo.

for letra in texto: #Para cada letra(variável que estou criando), em texto(iteravel), exiba a letra.
    print(letra)